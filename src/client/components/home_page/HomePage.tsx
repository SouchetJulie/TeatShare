import { FunctionComponent } from "react";
import styles from "../../styles/home_page/index.module.scss";

const HomePage: FunctionComponent = (): JSX.Element => {
  return <div className={styles.homeContainer} />;
};

export default HomePage;
